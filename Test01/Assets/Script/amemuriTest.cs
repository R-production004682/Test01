using UnityEngine;

public class amemuriTest : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Hello Ruruyume!");    
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
